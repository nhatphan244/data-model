query = """
        SELECT 1
        FROM gov_master
        WHERE ward_id = ? AND ward_name = ? AND 
              district_id = ? AND district_name = ? AND 
              province_id = ? AND province_name = ?
        """