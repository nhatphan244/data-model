for column in 'RepresentativeId':
        if column in df.columns:
            for index, row in df.iterrows():
                RID = str(row[column]) 
                if pd.isna(RID) or RID == '':
                    continue
                if len(RID) == 9:
                    if not RID.isdigit():
                        errors.append(f"Lỗi ở dòng {index + 2}, {column}: Số CCCD/CMND không hợp lệ")
                elif len(RID) == 12:
                    if not RID.startswith('0') or not RID[1:].isdigit():
                        errors.append(f"Lỗi ở dòng {index + 2}, {column}: Số CCCD/CMND không hợp lệ")
                else:
                        errors.append(f"Lỗi ở dòng {index + 2}, {column}: Số CCCD/CMND không hợp lệ")

For this code, I need it to check for number, the number that in the cell must be 9 or 12 in length, if it is 9 in length, it can be any combination as long as the total length is 9, if it is 12, it has to start with 0, otherwise return the message error, fix it for me

