"""
        [out:json];
        area["name"="{province_name}"]->.province;
        area["name"="{district_name}"]->.district;
        area["name"="{ward_name}"]["boundary"="administrative"](area.district)(area.province)->.commune_level_town;
        relation(area.province)(area.district)(area.commune_level_town)["name"="{ward_name}"]["boundary"="administrative"];
       
        out body;
         >;
        out skel qt;
    """
