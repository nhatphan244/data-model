Here are my original code from a script call validation.py:
for index, row in df.iterrows():
        ward = row['Phường']
        ward_id = row['Mã Phường']
        district = row['Quận']
        district_id = row['Mã Quận']
        province = row['Tỉnh Thành']
        province_id = row['Mã Tỉnh Thành']
        latitude = row['Latitude']
        longitude = row['Longitude']
       district_name = split_district_name(district)
        ward_name = split_ward_name(ward)
        province_name = split_province_name(province)
        query = df_gadm.loc[
        (df_gadm["NAME_1"] == province_name) & 
        (df_gadm["NAME_2"] == district_name) & 
        (df_gadm["NAME_3"] == ward_name)]
        if not query.empty:
            geo = query['geometry']
            actual_result = out_of_bounds_check(latitude,longitude,geo)

here are my function:
def out_of_bounds_check (lat,lon,polygon_coords):
    point = Point(lon,lat)
    polygon = gpd.GeoSeries.from_wkt([polygon_coords]).iloc[0]
    return polygon.contains(point)

how can I properly pass the geo (which now should be a list since it is inside a df.itterrows)
