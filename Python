query = """
        SELECT w.name, d.name, p.name
        FROM wards w
        JOIN districts d ON w.district_id = d.id
        JOIN provinces p ON d.province_id = p.id
        WHERE w.id = ? AND d.id = ? AND p.id = ?
        """